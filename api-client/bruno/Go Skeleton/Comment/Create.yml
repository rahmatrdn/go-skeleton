info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/v1/comment"
  body:
    type: json
    data: |-
      {
        "body": "test",
        "post_id": 2
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
