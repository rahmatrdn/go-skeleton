info:
  name: Update
  type: http
  seq: 3

http:
  method: PUT
  url: "{{url}}/api/v1/comment/1"
  body:
    type: json
    data: |-
      {
        "body": "updated",
        "post_id": 2
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
