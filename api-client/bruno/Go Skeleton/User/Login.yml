info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/v1/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "asd@asd.asd",
        "password": "asdasd"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
