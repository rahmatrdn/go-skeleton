info:
  name: Check Token
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/v1/auth/check-token"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
