info:
  name: Create
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/v1/todo-list"
  body:
    type: json
    data: |-
      {
        "description": "test",
        "doing_at": "2026-06-01",
        "title": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
