info:
  name: Delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{url}}/api/v1/todo-list/15"
  body:
    type: json
    data: |-
      {
        "description": "test",
        "doing_at": "2026-06-01",
        "title": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
