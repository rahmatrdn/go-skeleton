info:
  name: Update
  type: http
  seq: 2

http:
  method: PUT
  url: "{{url}}/api/v1/todo-list/3"
  body:
    type: json
    data: |-
      {
        "description": "test",
        "doing_at": "2026-06-01",
        "title": ""
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
