info:
  name: Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{url}}/api/v1/posts/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
