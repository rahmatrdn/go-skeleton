info:
  name: Get Post Comments
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/api/v1/posts/2/comment"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
